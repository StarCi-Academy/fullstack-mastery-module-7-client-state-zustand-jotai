import type { NextConfig } from "next"

/**
 * Cấu hình Next.js — App Router mặc định, bật reactStrictMode.
 * (EN: Next.js config — default App Router, strict mode on.)
 */
const nextConfig: NextConfig = {
    reactStrictMode: true,
}

export default nextConfig
